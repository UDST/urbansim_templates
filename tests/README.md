Run the tests from this folder with `pytest`. Add `-s` to see printed output, or `--cov=urbansim_templates` for a coverage report.

The tests read and write files using paths relative to this folder (`data/`, `configs/`), so they need to be run from here.
