# Pip constraints pinning the oldest supported versions of the direct
# dependencies (the floors declared in pyproject.toml). Used by the
# minimum-dependencies CI job:
#
#   pip install ".[test]" -c requirements-min.txt
#
# The NumPy, Pandas, and Statsmodels floors match those of UrbanSim 3.3,
# which is the oldest UrbanSim release that runs on current versions of
# those libraries. tables is an Orca dependency and is pinned so the
# lower-bound environment stays reproducible.

choicemodels == 0.3
numpy == 1.26.*
orca == 1.8
pandas == 2.2.*
patsy == 0.5.*
statsmodels == 0.14.*
tables == 3.8.*
urbansim == 3.3
